import { XcErrorImageDirective } from './xc-error-image.directive';

describe('XcErrorImageDirective', () => {
  it('should create an instance', () => {
    const directive = new XcErrorImageDirective();
    expect(directive).toBeTruthy();
  });
});
